#####install
`npm install`
#####run server
`npm start`

`$env:PORT=4000; $env:DB_CONFIGURATION_PASSWORD='1111'; npm start;`

or

`node ./bin/www`

#####run just the file
`node ./path/to/file.js`

for example:

`node  node .\try\a.js`
