<Window x:Class="UBA6_Controller_App.BK_Precision9104Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UBA6_Controller_App"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        xmlns:vm ="clr-namespace:UBA6_Controller_App.ViewModel"
        d:DataContext="{d:DesignInstance Type=vm:BK_Precision9104ViewModel}"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Grid d:ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menu -->
            <RowDefinition Height="Auto"/>
            <!-- Frames -->
            <RowDefinition Height="*"/>
            <!-- TabControl -->
            <RowDefinition Height="Auto"/>
            <!-- StatusBar -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <MenuItem Header="_File">
                <MenuItem Header="Open"/>
                <MenuItem Header="Exit"/>
            </MenuItem>
            <MenuItem Header="Settings">
                <MenuItem Header="Select COM Port" >
                    <ComboBox x:Name="ComPortComboBox" Width="150"  SelectedItem="{Binding SelectedPort}" ItemsSource="{Binding Ports}" />
                </MenuItem>                
                <Button Content="Query All" Command="{Binding QueryAllCommand}"/>
                <Button Content="Sync" Command="{Binding UpdateVMCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="About"/>
            </MenuItem>
        </Menu>
        <Grid Grid.Row="2" d:ShowGridLines="True" Name="GridMain">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ui:NumberBox  Grid.Row="0" Grid.Column="0" Header="Voltage [mV]:" IsEnabled="{Binding IsEditable}" d:Value="5" Value="{Binding Voltage}" Style ="{StaticResource BK9104StyleNumberBox}"/>
            <ui:NumberBox  Grid.Row="0" Grid.Column="1" Header="UVL [mV]:" d:Value="5" Value="{Binding UVL}" Style ="{StaticResource BK9104StyleNumberBox}"/>
            <ui:NumberBox  Grid.Row="0" Grid.Column="2"  Header="Current [mA]:" IsEnabled="{Binding IsEditable}" d:Value="5" Value="{Binding Current}" Style="{StaticResource BK9104StyleNumberBox}" />
            <ui:NumberBox  Grid.Row="0" Grid.Column="3"  Header="UCL [mA]:" d:Value="5" Value="{Binding UCL}" Style="{StaticResource BK9104StyleNumberBox}" />
            <ui:RadioButtons Grid.Row="0" Grid.Column="4" CacheMode="BitmapCache" Header="Mode" >
                <RadioButton Content="CV"  IsChecked="{Binding IsCV_Mode}" GroupName="CC_CV_Mode" IsHitTestVisible ="False" />
                <RadioButton Content="CC"  IsChecked="{Binding IsCC_Mode}" GroupName="CC_CV_Mode" IsHitTestVisible ="False"/>
            </ui:RadioButtons>
        <Grid  Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="0"  Orientation="Vertical" >
                    <RadioButton Content="Preset A" Name="RadioButton_PresetA" IsChecked="{Binding IsPresetSelect[0]}" GroupName="Preset" Checked="RadioButton_PresetA_Checked" HorizontalAlignment="Center"/>
                    <ui:NumberBox  Header="Voltage [mV]:" d:Value="5" Value="{Binding PresetVoltage[0]}" Style="{StaticResource BK9104StyleNumberBox}" />
                    <ui:NumberBox  Header="Current [mA]:" d:Value="5" Value="{Binding PresetCurrent[0]}" Style="{StaticResource BK9104StyleNumberBox}" />
                    <Button Content="Set" HorizontalAlignment="Stretch" Margin="10" VerticalAlignment="Center" HorizontalContentAlignment="Center" Command="{Binding SetPreset1ConfigCommand}"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1"  Orientation="Vertical" >
                    <RadioButton Content="Preset B" Name="RadioButton_PresetB" IsChecked="{Binding IsPresetSelect[1]}" GroupName="Preset" Checked="RadioButton_PresetA_Checked" HorizontalAlignment="Center" />
                    <ui:NumberBox  Header="Voltage [mV]:" d:Value="5" Value="{Binding PresetVoltage[1]}" Style="{StaticResource BK9104StyleNumberBox}" />
                    <ui:NumberBox  Header="Current [mA]:" d:Value="5" Value="{Binding PresetCurrent[1]}" Style="{StaticResource BK9104StyleNumberBox}" />
                    <Button Content="Set" HorizontalAlignment="Stretch" Margin="10" VerticalAlignment="Center" HorizontalContentAlignment="Center" Command="{Binding SetPreset2ConfigCommand}"/>
                </StackPanel>
                <StackPanel  Grid.Row="0" Grid.Column="2" Orientation="Vertical">
                    <RadioButton Content="Preset C" Name="RadioButton_PresetC" IsChecked="{Binding IsPresetSelect[2]}" GroupName="Preset" Checked="RadioButton_PresetA_Checked" HorizontalAlignment="Center" />
                    <ui:NumberBox  Header="Voltage [mV]:" d:Value="5" Value="{Binding PresetVoltage[2]}" Style="{StaticResource BK9104StyleNumberBox}" />
                    <ui:NumberBox  Header="Current [mA]:" d:Value="5" Value="{Binding PresetCurrent[2]}" Style="{StaticResource BK9104StyleNumberBox}" />
                    <Button Content="Set" HorizontalAlignment="Stretch" Margin="10" VerticalAlignment="Center" HorizontalContentAlignment="Center" Command="{Binding SetPreset3ConfigCommand}"/>
                </StackPanel>


             
            </Grid>
            <Grid Grid.Row="1" Grid.Column="5" d:ShowGridLines="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:ToggleSwitch Grid.Row ="0"  Grid.Column="0" Header="Output:" OffContent="Off"  OnContent="On" IsOn="{Binding IsOutputOn}" d:IsOn="True" />
            <ui:ToggleSwitch Grid.Row ="1"  Grid.Column="0" Header="Auto Read:" OffContent="Off"  OnContent="On" IsOn="{Binding IsAutoRead}" d:IsOn="True" />
                
            </Grid>
        </Grid>
            <StatusBar Grid.Row="3" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- Status message -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Ellipse Width="10" Height="10" Fill="{Binding FillColor}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding StatusTime, Converter={StaticResource DateTimeConvertor}}" VerticalAlignment="Center"/>
                        <Border BorderThickness="2" />
                        <TextBlock d:Text="Blalba" Text="{Binding Status}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </StatusBar>
            <StatusBar Grid.Row="3" HorizontalAlignment="Right" HorizontalContentAlignment="Stretch">
                <ProgressBar Grid.Column="2" Width="150" Height="16" Value="40" Maximum="100" VerticalAlignment="Center"/>
            </StatusBar>
        </Grid>
</Window>
